import { View, Text } from 'react-native'
import React from 'react'

const PageB = ({navigation }) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>This is B page</Text>
    </View>
  )
}

export default PageB